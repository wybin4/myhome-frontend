import { Htag } from "@/components";
import { withLayout } from "@/layout/Layout";

function Event(): JSX.Element {
    return (
        <>
            <Htag size="h1">События</Htag>

        </>
    );
}

export default withLayout(Event);